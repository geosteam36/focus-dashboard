{
  "name": "focus-dashboard",
  "version": "0.1.0",
  "description": "A Pomodoro timer, Schulte Table focus trainer, and dev resource locker in one dashboard.",
  "type": "module",
  "scripts": {
    "dev": "npx serve . -l 5173",
    "test": "vitest run",
    "test:watch": "vitest","build": "echo 'No build step required for static ES modules'",
    "preview": "npx serve . -l 5173"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "vitest": "^4.1.10"
  }
}
